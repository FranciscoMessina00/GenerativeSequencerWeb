{
  "name": "web-generative-sequencer",
  "version": "0.1.0",
  "description": "Generative Euclidean sequencer with a modal string voice, ported to the Web Audio API from the CMLS Stefano Lavori SuperCollider/Processing project.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "serve": "python3 -m http.server 8080"
  }
}
