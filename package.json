{
  "name": "web-generative-sequencer",
  "version": "0.1.0",
  "description": "Generative Euclidean sequencer driving a modal string voice, built on the Web Audio API.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "serve": "python3 -m http.server 8080"
  }
}
